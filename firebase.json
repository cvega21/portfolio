{
  "database": {
    "rules": "database.rules.json"
  },
  "functions": {
    "predeploy": [
      "cd \"$RESOURCE_DIR\" && yarn run lint",
      "cd \"$RESOURCE_DIR\" && yarn run build"

    ]
  }
}
