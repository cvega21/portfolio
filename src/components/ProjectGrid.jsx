import React from 'react'

const ProjectGrid = () => {
  // for each project given here as an array, render it below
  
  return (
    <div className="ProjectsGrid">
      {/* props.projectArr.map(()=> {
        <Project ></Project>
      }) */}
    </div>
  )
}

export default ProjectGrid
